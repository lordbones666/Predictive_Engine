"""Sequence models placeholder for future work."""

from __future__ import annotations


class SequenceModel:
    def fit(self) -> None:
        raise NotImplementedError("Sequence models are intentionally omitted in the initial build.")
